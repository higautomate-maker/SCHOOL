name: hig_driver_gps
description: Hig School driver app with secure transporter identity and foreground-only Stage 9 trip tracking.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=3.5.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  hig_mobile_core:
    path: ../packages/hig_mobile_core
  geolocator: ^13.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
