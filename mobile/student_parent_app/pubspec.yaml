name: hig_student_parent
description: Hig School Student and Parent app with secure mobile identity, offline sync, and push registration.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=3.5.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  hig_mobile_core:
    path: ../packages/hig_mobile_core
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
