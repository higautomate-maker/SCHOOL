services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: hig_school_test
      POSTGRES_USER: hig_school_test
      POSTGRES_PASSWORD: hig_school_test
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hig_school_test -d hig_school_test"]
      interval: 2s
      timeout: 3s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data

  redis:
    image: redis:7.4-alpine
    ports:
      - "56379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 3s
      retries: 20
    tmpfs:
      - /data
