"use client";import{FormEvent,useState}from"react";export default function Accept(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const token=new URLSearchParams(location.search).get("token")??"";const r=await fetch("/api/v1/auth/invitations/accept",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,email,password})});const j=await r.json() as{error?:string};setMessage(r.ok?"Invitation accepted. You can now sign in.":j.error??"Invitation failed");}return <main><h1>Accept school invitation</h1><form onSubmit={submit}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" minLength={12} maxLength={128} value={password} onChange={e=>setPassword(e.target.value)} required/><button>Accept invitation</button></form>{message&&<p>{message}</p>}</main>}
