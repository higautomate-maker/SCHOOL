"use client";
import {FormEvent,useState}from"react";import styles from"./login.module.css";
export default function LoginPage(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");async function signIn(event:FormEvent){event.preventDefault();setBusy(true);setError("");try{const returnTo=new URLSearchParams(location.search).get("returnTo")??undefined;const response=await fetch("/api/v1/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password,returnTo})});const result=await response.json() as{error?:string;destination?:string};if(!response.ok)throw new Error(result.error??"Sign in failed");location.assign(result.destination??"/");}catch(cause){setError(cause instanceof Error?cause.message:"Sign in failed");}finally{setBusy(false);}}return <main className={styles.loginPage}><section className={styles.brandPanel}><div className={styles.brand}><i>H</i><span><b>Hig School</b><small>HIG AUTOMATION INDIA PRIVATE LIMITED</small></span></div><div className={styles.promise}><span>SECURE SCHOOL ECOSYSTEM</span><h1>One verified identity for every school role.</h1><p>Your school, role and permissions are resolved securely after sign-in.</p></div></section><section className={styles.loginPanel}><header><span>SECURE ACCESS</span><h2>Sign in to Hig School</h2><p>Use the account created by your company or school administrator.</p></header><form onSubmit={signIn}><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" minLength={12} maxLength={128} required/></label>{error&&<p className={styles.error}>{error}</p>}<button disabled={busy}>{busy?"Signing in…":"Sign in"}<span>→</span></button></form><p className={styles.note}><a href="/password/forgot">Forgot your password?</a></p></section></main>}
