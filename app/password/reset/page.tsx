"use client";import{FormEvent,useState}from"react";export default function Reset(){const[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const token=new URLSearchParams(location.search).get("token")??"";const r=await fetch("/api/v1/auth/password/reset",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})});const j=await r.json() as{error?:string};setMessage(r.ok?"Password reset. You can now sign in.":j.error??"Reset failed");}return <main><h1>Choose a new password</h1><form onSubmit={submit}><input type="password" minLength={12} maxLength={128} value={password} onChange={e=>setPassword(e.target.value)} required/><button>Reset password</button></form>{message&&<p>{message}</p>}</main>}
