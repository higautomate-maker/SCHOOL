"use client";import{FormEvent,useState}from"react";export default function Forgot(){const[email,setEmail]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await fetch("/api/v1/auth/password/forgot",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const j=await r.json() as{message:string};setMessage(j.message);}return <main><h1>Forgot password</h1><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button>Send reset instructions</button></form>{message&&<p>{message}</p>}</main>}
